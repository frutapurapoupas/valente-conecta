// app/empregos/page.tsx - REFATORADO (APENAS 4 LINHAS)

"use client";

import { EmpregosContainer } from "./containers/EmpregosContainer";

export default function EmpregosPage() {
  return <EmpregosContainer />;
}