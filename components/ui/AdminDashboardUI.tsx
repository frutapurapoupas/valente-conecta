"use client";

// Legacy stub — use components/admin-ui/AdminDashboardUI instead
export default function AdminDashboardUI(_props: { summary?: unknown; charts?: unknown; reports?: unknown }) {
  return null
}
