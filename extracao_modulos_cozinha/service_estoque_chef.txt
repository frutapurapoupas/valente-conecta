========================================
ARQUIVO: app/admin-master/cozinha-chef/services/estoqueService.ts
MÓDULO: service
DATA: 2026-07-20 22:08:01
========================================
import { supabase } from '@/lib/supabase/client';

export const estoqueService = {
  async listar() {
    const { data, error } = await supabase
      .from('estoque')
      .select('*')
      .order('nome', { ascending: true });
    
    if (error) throw new Error(error.message);
    return data || [];
  },

  async buscarPorId(id: string) {
    const { data, error } = await supabase
      .from('estoque')
      .select('*')
      .eq('id', id)
      .single();
    
    if (error) throw new Error(error.message);
    return data;
  },

  async criar(dados: any) {
    const { data, error } = await supabase
      .from('estoque')
      .insert([dados])
      .select()
      .single();
    
    if (error) throw new Error(error.message);
    return data;
  },

  async atualizar(id: string, dados: any) {
    const { data, error } = await supabase
      .from('estoque')
      .update(dados)
      .eq('id', id)
      .select()
      .single();
    
    if (error) throw new Error(error.message);
    return data;
  },

  async deletar(id: string) {
    const { error } = await supabase
      .from('estoque')
      .delete()
      .eq('id', id);
    
    if (error) throw new Error(error.message);
    return true;
  }
};


