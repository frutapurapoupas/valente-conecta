========================================
ARQUIVO: src\modules\cozinha\services\PedidoService.ts
DATA: 07/20/2026 21:56:58
========================================

// app/cozinha/services/PedidoService.ts
import { PedidoRepository } from '../repositories/PedidoRepository';
import { Pedido, PedidoInput, PedidoStatus } from '../types/pedido';

export class PedidoService {
  private repository = new PedidoRepository();

  async listarTodos(): Promise<Pedido[]> {
    try {
      return await this.repository.buscarTodos();
    } catch (error) {
      console.error('Erro no Service ao listar pedidos:', error);
      throw error;
    }
  }

  async listarPorStatus(status: PedidoStatus): Promise<Pedido[]> {
    try {
      if (!status) {
        throw new Error('Status Ã© obrigatÃ³rio');
      }
      return await this.repository.buscarPorStatus(status);
    } catch (error) {
      console.error('Erro no Service ao listar pedidos por status:', error);
      throw error;
    }
  }

  async listarPorUsuario(usuarioId: string): Promise<Pedido[]> {
    try {
      if (!usuarioId) {
        throw new Error('ID do usuÃ¡rio Ã© obrigatÃ³rio');
      }
      return await this.repository.buscarPorUsuario(usuarioId);
    } catch (error) {
      console.error('Erro no Service ao listar pedidos do usuÃ¡rio:', error);
      throw error;
    }
  }

  async criar(pedido: PedidoInput): Promise<Pedido> {
    try {
      // Regra de negÃ³cio: Validar pedido
      this.validarPedido(pedido);

      // Regra de negÃ³cio: Pedido deve ter pelo menos um item
      if (pedido.items.length === 0) {
        throw new Error('Pedido deve ter pelo menos um item');
      }

      return await this.repository.criar(pedido);
    } catch (error) {
      console.error('Erro no Service ao criar pedido:', error);
      throw error;
    }
  }

  async atualizarStatus(id: string, status: PedidoStatus): Promise<Pedido> {
    try {
      if (!id) {
        throw new Error('ID do pedido Ã© obrigatÃ³rio');
      }

      if (!status) {
        throw new Error('Status Ã© obrigatÃ³rio');
      }

      // Regra de negÃ³cio: Validar transiÃ§Ã£o de status
      const pedido = await this.repository.buscarTodos();
      const encontrado = pedido.find(p => p.id === id);

      if (!encontrado) {
        throw new Error('Pedido nÃ£o encontrado');
      }

      // NÃ£o permitir cancelar pedidos jÃ¡ entregues
      if (encontrado.status === 'entregue' && status === 'cancelado') {
        throw new Error('NÃ£o Ã© possÃ­vel cancelar um pedido jÃ¡ entregue');
      }

      return await this.repository.atualizarStatus(id, status);
    } catch (error) {
      console.error('Erro no Service ao atualizar status do pedido:', error);
      throw error;
    }
  }

  async cancelar(id: string): Promise<Pedido> {
    try {
      if (!id) {
        throw new Error('ID do pedido Ã© obrigatÃ³rio');
      }
      return await this.repository.cancelar(id);
    } catch (error) {
      console.error('Erro no Service ao cancelar pedido:', error);
      throw error;
    }
  }

  private validarPedido(pedido: PedidoInput): void {
    if (!pedido.usuarioId) {
      throw new Error('ID do usuÃ¡rio Ã© obrigatÃ³rio');
    }

    if (!pedido.items || pedido.items.length === 0) {
      throw new Error('Pedido deve ter pelo menos um item');
    }

    for (const item of pedido.items) {
      if (!item.produtoId) {
        throw new Error('ID do produto Ã© obrigatÃ³rio em cada item');
      }
      if (item.quantidade <= 0) {
        throw new Error('Quantidade deve ser maior que zero');
      }
      if (item.precoUnitario < 0) {
        throw new Error('PreÃ§o unitÃ¡rio nÃ£o pode ser negativo');
      }
    }
  }
}

