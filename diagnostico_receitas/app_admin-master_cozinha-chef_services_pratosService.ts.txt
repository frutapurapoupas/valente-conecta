========================================
ARQUIVO: app\admin-master\cozinha-chef\services\pratosService.ts
DATA: 07/20/2026 21:56:56
========================================

import { supabase } from '@/lib/supabase/client';

export const pratosService = {
  async listar() {
    const { data, error } = await supabase
      .from('pratos')
      .select('*')
      .order('nome', { ascending: true });

    if (error) throw new Error(error.message);
    return data || [];
  },

  async buscarPorId(id: string) {
    const { data, error } = await supabase
      .from('pratos')
      .select('*')
      .eq('id', id)
      .single();

    if (error) throw new Error(error.message);
    return data;
  },

  async criar(dados: any) {
    const { data, error } = await supabase
      .from('pratos')
      .insert([dados])
      .select()
      .single();

    if (error) throw new Error(error.message);
    return data;
  },

  async atualizar(id: string, dados: any) {
    const { data, error } = await supabase
      .from('pratos')
      .update(dados)
      .eq('id', id)
      .select()
      .single();

    if (error) throw new Error(error.message);
    return data;
  },

  async deletar(id: string) {
    const { error } = await supabase
      .from('pratos')
      .delete()
      .eq('id', id);

    if (error) throw new Error(error.message);
    return true;
  }
};



