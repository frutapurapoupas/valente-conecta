========================================
ARQUIVO: app\admin-master\cozinha-chef\producao\page.tsx
DATA: 07/20/2026 21:56:56
========================================

'use client';

import { Factory, RefreshCw } from 'lucide-react';
import { useProducao } from '../hooks/useProducao';

const STATUS_LABEL: Record<string, string> = {
  planejado: 'Planejado',
  em_producao: 'Em produÃ§Ã£o',
  concluido: 'ConcluÃ­do',
  cancelado: 'Cancelado',
};

export default function ProducaoPage() {
  const { producoes, loading, error, fetchProducoes } = useProducao();

  if (loading) {
    return (
      <div className="flex min-h-screen items-center justify-center">
        <div className="h-10 w-10 animate-spin rounded-full border-b-2 border-blue-500" />
      </div>
    );
  }

  if (error) {
    return (
      <div className="p-6">
        <div className="rounded-lg border border-red-200 bg-red-50 p-4 text-red-700">
          Erro ao carregar produÃ§Ã£o: {error}
        </div>
      </div>
    );
  }

  return (
    <div className="p-6">
      <div className="mb-6 flex items-center justify-between">
        <div>
          <h1 className="flex items-center gap-2 text-2xl font-bold">
            <Factory size={24} /> ProduÃ§Ã£o
          </h1>
          <p className="text-sm text-gray-600">Acompanhamento das ordens de produÃ§Ã£o.</p>
        </div>
        <button
          onClick={fetchProducoes}
          className="rounded-lg border border-gray-300 px-3 py-2 text-sm hover:bg-gray-50"
        >
          <span className="flex items-center gap-2">
            <RefreshCw size={16} /> Atualizar
          </span>
        </button>
      </div>

      <div className="overflow-hidden rounded-lg border bg-white">
        <table className="min-w-full divide-y divide-gray-200">
          <thead className="bg-gray-50">
            <tr>
              <th className="px-4 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500">Data</th>
              <th className="px-4 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500">Prato</th>
              <th className="px-4 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500">Prevista</th>
              <th className="px-4 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500">Produzida</th>
              <th className="px-4 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500">Status</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-gray-200 bg-white">
            {producoes.length === 0 ? (
              <tr>
                <td colSpan={5} className="px-4 py-8 text-center text-sm text-gray-500">
                  Nenhum registro de produÃ§Ã£o.
                </td>
              </tr>
            ) : (
              producoes.map((producao) => (
                <tr key={producao.id}>
                  <td className="px-4 py-3 text-sm text-gray-700">
                    {new Date(producao.data_producao).toLocaleDateString('pt-BR')}
                  </td>
                  <td className="px-4 py-3 text-sm font-medium text-gray-900">{producao.prato_id}</td>
                  <td className="px-4 py-3 text-sm text-gray-700">{producao.quantidade_prevista}</td>
                  <td className="px-4 py-3 text-sm text-gray-700">{producao.quantidade_produzida}</td>
                  <td className="px-4 py-3 text-sm text-gray-700">
                    {STATUS_LABEL[producao.status] || producao.status}
                  </td>
                </tr>
              ))
            )}
          </tbody>
        </table>
      </div>
    </div>
  );
}


