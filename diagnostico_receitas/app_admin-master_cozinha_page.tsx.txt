========================================
ARQUIVO: app\admin-master\cozinha\page.tsx
DATA: 07/20/2026 21:56:55
========================================

// app/admin-master/cozinha/page.tsx
"use client";

import { DashboardStats } from './components/DashboardStats';
import { ReceitaList } from './components/ReceitaList';
import { EstoqueList } from './components/EstoqueList';
import { useReceitas } from '@/src/modules/cozinha/hooks/useReceitas';
import { useEstoque } from './hooks/useEstoque';
import { useFinanceiroCozinha } from './hooks/useFinanceiroCozinha';

export default function AdminCozinhaPage() {
  const { receitas, loading: loadingReceitas } = useReceitas();
  const { resumo, alertas, loading: loadingEstoque } = useEstoque();
  const { resumo: financeiro, loading: loadingFinanceiro } = useFinanceiroCozinha();

  const receitasAtivas = receitas.filter(r => r.ativo !== false);
  const totalIngredientes = resumo.length;

  return (
    <div className="p-6 space-y-6">
      <div className="flex justify-between items-center">
        <div>
          <h1 className="text-2xl font-bold text-gray-800">ðŸ‘¨â€ðŸ³ AdministraÃ§Ã£o da Cozinha</h1>
          <p className="text-gray-500 mt-1">Gerencie receitas, estoque, produÃ§Ã£o e finanÃ§as</p>
        </div>
      </div>

      {/* Stats */}
      <DashboardStats
        totalReceitas={receitasAtivas.length}
        totalIngredientes={totalIngredientes}
        alertasEstoque={alertas.length}
        producaoHoje={0}
        faturamentoMes={financeiro?.totalReceitas || 0}
        loading={loadingReceitas || loadingEstoque || loadingFinanceiro}
      />

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {/* Receitas */}
        <div>
          <h2 className="text-lg font-semibold text-gray-700 mb-3">ðŸ“‹ Ãšltimas Receitas</h2>
          <ReceitaList
            receitas={receitasAtivas.slice(0, 4)}
            loading={loadingReceitas}
          />
        </div>

        {/* Estoque */}
        <div>
          <h2 className="text-lg font-semibold text-gray-700 mb-3">ðŸ“¦ Resumo do Estoque</h2>
          <EstoqueList
            resumo={resumo.slice(0, 5)}
            loading={loadingEstoque}
          />
        </div>
      </div>

      {/* Alertas de Estoque */}
      {alertas.length > 0 && (
        <div className="bg-red-50 border border-red-200 rounded-lg p-4">
          <h3 className="text-red-700 font-semibold">ðŸš¨ Alertas de Estoque</h3>
          <ul className="mt-2 space-y-1">
            {alertas.map(item => (
              <li key={item.ingredienteId} className="text-sm text-red-600">
                {item.ingredienteNome}: {item.quantidadeAtual} unidades (mÃ­nimo: {item.estoqueMinimo})
              </li>
            ))}
          </ul>
        </div>
      )}
    </div>
  );
}

