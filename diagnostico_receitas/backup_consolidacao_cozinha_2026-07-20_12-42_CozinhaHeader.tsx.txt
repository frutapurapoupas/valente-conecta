========================================
ARQUIVO: backup_consolidacao_cozinha_2026-07-20_12-42\CozinhaHeader.tsx
DATA: 07/20/2026 21:56:57
========================================

"use client";

// ==================================================
// MÃ“DULO COZINHA - COMPONENTE HEADER
// ==================================================

interface CozinhaHeaderProps {
  title: string;
  subtitle?: string;
  onRefresh?: () => void;
  onSearch?: (query: string) => void;
}

export function CozinhaHeader({ 
  title, 
  subtitle, 
  onRefresh,
  onSearch 
}: CozinhaHeaderProps) {
  return (
    <div className="bg-gradient-to-r from-blue-600 to-blue-800 rounded-xl p-6 mb-6 shadow-lg">
      <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
        <div>
          <h1 className="text-2xl font-bold text-white">{title}</h1>
          {subtitle && (
            <p className="text-blue-100 text-sm mt-1">{subtitle}</p>
          )}
        </div>
        
        <div className="flex items-center gap-3 w-full md:w-auto">
          {onSearch && (
            <div className="flex-1 md:flex-none">
              <input
                type="text"
                placeholder="Buscar produtos..."
                onChange={(e) => onSearch(e.target.value)}
                className="w-full md:w-64 px-4 py-2 rounded-lg text-gray-700 bg-white/90 focus:bg-white focus:outline-none focus:ring-2 focus:ring-blue-300 transition-all"
              />
            </div>
          )}
          
          {onRefresh && (
            <button
              onClick={onRefresh}
              className="px-4 py-2 bg-white/20 text-white rounded-lg hover:bg-white/30 transition-colors flex items-center gap-2"
            >
              <svg
                className="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeWidth={2}
                  d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"
                />
              </svg>
              Atualizar
            </button>
          )}
        </div>
      </div>
    </div>
  );
}

